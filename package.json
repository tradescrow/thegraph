{
  "name": "tradescrow",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy:avalanche": "graph deploy --node https://api.studio.thegraph.com/deploy/ --network=avalanche --version-label=v1.0.2 tradescrow-avalanche",
    "deploy:arbitrum": "graph deploy --node https://api.studio.thegraph.com/deploy/ --network=arbitrum-one --version-label=v1.0.2 tradescrow-arbitrum",
    "deploy:mainnet": "graph deploy --node https://api.studio.thegraph.com/deploy/ --network=mainnet --version-label=v1.0.2 tradescrow-mainnet",
    "create-local": "graph create --node http://localhost:8020/ tradescrow",
    "remove-local": "graph remove --node http://localhost:8020/ tradescrow",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 tradescrow",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.51.2",
    "@graphprotocol/graph-ts": "0.30.0"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
